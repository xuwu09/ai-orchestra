# 工作流模板总索引

> 接单先扫此表：任务特征命中 → 调对应模板（`workflows/` 下同名文件），替换变量直接派发。
> 新模板建成后必须在此登记一行。模板跑完有改进要回写，并更新「最后实跑」日期。

| 模板 | 任务特征 | 调用链摘要 | 最后实跑 |
|---|---|---|---|
| example-建站项目.md | 网站类项目（静态站/工具站，file:// 或本地落盘交付） | 顾问组方案多轮互审 → 干活组落盘 → 组长静态+行为验收 → 修复循环 | 示例（源项目：健身指导网站） |

## 待沉淀（跑完还没来得及写成模板的）
- （无）
