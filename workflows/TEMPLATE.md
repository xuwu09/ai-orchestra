# 工作流模板骨架

> 沉淀新模板时复制本文件，文件名 `YYYYMMDD-<slug>.md`，并在 INDEX.md 登记。
> 只记「换个项目还能照跑」的内容，不记一次性的具体文案。

```
【工作流模板】<名称>
- 沉淀日期：YYYY-MM-DD
- 来源项目：<项目名 / 任务编号>

## 任务特征（触发条件）
- 什么任务该套这个模板：<特征 1；特征 2；…>
- 什么任务不要套：<反例>

## 调用链
<组/成员 → 组/成员 → …，标注并行段与串行依赖>
例：顾问组（千问主笔 ↔ 豆包互审×3）→ 程序组（X 落盘）→ 组长验收 → 修复循环

## 各环任务包要点
- 方案环：<本模板特有的要求（复用 playbooks 对应剧本）>
- 制作环：<落盘路径约定、自测要求、交付说明格式、执行模式（plan/normal/trust）>
- 验收环：<必须写成可执行命令，不是描述——如 `npm run typecheck && npm test`、`ffprobe -i 成片.mp4`、自定义校验脚本路径；人眼只查机器查不了的>

## 验收要点与坑记录
- <坑 1：现象 → 原因 → 规避方式>
- <坑 2：…>

## 可替换变量清单（复用时只改这些）
| 变量 | 说明 | 本次取值（示例） |
|---|---|---|
| PROJECT_NAME | 项目名 | … |
| OUTPUT_DIR | 产物落盘目录 | … |
| … | … | … |

## Golden 样本（用户终验通过后固化，复用时对比回归）
- 产物快照：<关键文件结构清单 / 目录树>
- 验收命令标准输出：<如 `npm test` 通过时的输出摘要、`ffprobe` 参数（时长/分辨率/大小）>
- 偏离处理：复用时结果与样本明显偏离（结构缺失、输出异常、参数落差）→ 先排查（依赖漂移/通道变化/成员更换）再继续，不带病续跑

## 实跑记录（每次复用追加一行）
- YYYY-MM-DD：<项目>，<结论（通过/返工 N 轮）/模板改进点>
```
