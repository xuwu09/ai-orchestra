---
name: ai-orchestra
description: 多 AI 协作调度编排技能。当用户要求「调度多个 AI」「开工作组」「让千问/豆包/Codex/Claude Code/GPT/Grok/Gemini/Kimi/DSH 等协作完成任务」「多智能体流水线」「AI 分工」「更换组长」「生图/生视频/设计/代码组派单」「ChatParty 搜索组/方案组调度」时使用。角色制架构（成员可插拔）——组长层（任何能改本地文件+派单验收的客户端：WorkBuddy/Codex/Claude Code/Pi/Harness 等）、顾问组（只说不干的 AI：千问/豆包/GPT/Grok/Gemini/Qwen/Kimi/MiniMax/智谱清言/网页 AI 等，含 ChatParty 搜索组[元宝/豆包网页/文心/纳米/秘塔，无限额]与方案组[GLM/MiMo/Kimi/千问，限额只干核心]）、干活组（任何能修改本地文件的 agent：DSH/Codex/Claude Code/Pi/Harness 等）、特殊工作组（与干活组同级：本机组 Marvis/ima、创意组[生图生视频+设计合并] 即梦/可灵/ComfyUI/SD/VibePaper/Updream/Miora/Wegic/Pixso/Figma、代码组[原程序组] Kimi Code/MiMo Code/TRAE/Cursor/ZCode/OpenCode）——含主流程（方案多轮互审→制作→验收→修复循环；纯文字出品走「搜索组集资料→方案组讨论→指定一位定稿交付」快车道）、成员管理与各 AI 能力档案。
---

# AI Orchestra（多 AI 交响调度）

## 架构总览：角色制（成员可插拔）

用户（2026-09-15 四次修订）确定的架构：**先定角色，再填成员**。角色靠能力准入，名单随时增删（见「成员管理」）：

```
┌─────────────── 组长层（可更换）───────────────────┐
│   准入：能修改本地文件 + 能派单收产物 + 能实测验收 │
│   现役：WorkBuddy（默认）                          │
│   候补：Codex / Claude Code / Pi / Harness 等；   │
│         代码组全员（Kimi Code / MiMo Code /       │
│         TRAE / Cursor / ZCode / OpenCode）均可    │
│   职责：接单 → 拆解 → 派单 → 监控 → 验收 → 交付    │
│   产物验收权在组长：干活组做完先交组长实测，        │
│   组长通过后才交用户最终验收                        │
└──────┬─────────────────────┬──────────────────────┘
       │ 派出方案任务包        │ 派出实现/修复任务包
       ▼                     ▼
┌── 顾问组（可插拔）───────────┐  ┌── 干活组（可插拔）──────┐
│ 准入：能对话产出长方案       │  │ 准入：能修改本地文件落盘 │
│ 现役：千问 · 豆包           │  │ 现役：DSH               │
│ ── ChatParty 搜索组(无限额)─│  │ 候补：Codex / Claude    │
│   元宝 豆包网页 文心        │  │ Code / Pi / Harness 等  │
│   纳米 秘塔                │  │ 职责：按定稿方案制作；   │
│ ── ChatParty 方案组(限额) ──│  │ 按修复方案修 bug         │
│   GLM MiMo Kimi 千问       │  └─────────────────────────┘
│ 候补：GPT / Grok / Gemini / │
│ Qwen / 网页AI 等           │
└─────────────────────────────┘

┌── 特殊工作组（与干活组同级；特定任务才启用；成员可插拔）──────────┐
│ 本机组：Marvis（本机文件/系统/跨端执行）                          │
│         ima（知识库入库/RAG/公众号素材/报告播客）                  │
│ 创意组（原生图生视频组 + 设计组合并，2026-09-15）：                │
│   生图生视频：即梦AI · 可灵AI · ComfyUI · VibePaper ·             │
│              Updream · Stable Diffusion                          │
│   设计：Miora · Wegic · Pixso · Figma                            │
│ 代码组（原程序组）：Kimi Code · MiMo Code · TRAE · Cursor ·       │
│         ZCode · OpenCode（准入同干活组，按任务特征分流）           │
└──────────────────────────────────────────────────────────────────┘

直连权限（2026-09-15 放开）：顾问组 ↔ 顾问组（互审/讨论）；顾问组 → 干活组；
顾问组 → 组长；顾问组 → 特殊工作组；组长 ↔ 全部组。
直连只省中转，验收权不变：一切产物终验仍归 组长实测 → 用户终验。
```

**特殊工作组启用规则（与干活组同级，组长或顾问组均可直接对接）**：
- **创意组 · 专属通道**：各成员的专属 MCP/CLI（可灵 MCP、dreamina-cli、Moira、figma-cli、ComfyUI MCP、sd-webui-mcp、PaperMCP 等）**已登记未安装**（清单见 capabilities.md 创意组段首），用户点名需要时再装，装完把状态改「已装」。未装时走默认通道（网页接力 / CLI-Anything）。
- **创意组 · 生图生视频方向**：任务需要图像/视频产出时启用。组长优先用内置生图/生视频能力（快、免费、可控）；对质量/风格/分辨率有更高要求、或需要图生视频/首尾帧/本地工作流时，派媒体组成员——**通道按「网页 vs 程序」二分**：即梦/可灵/VibePaper/Updream 是网页 → opencli browser 控制；ComfyUI/SD 是本地程序 → CLI-Anything 控制。产物先交组长核验（尺寸/内容/风格是否符合方案），再交用户。
- **创意组 · 设计方向**：任务需要 UI 设计稿、配色规范、原型图、可交互设计稿时启用。产出（导出的图片/标注/设计文件路径）作为顾问组方案的「设计附录」并入定稿，干活组照稿实现。组长核对设计稿与方案约束（尺寸/组件清单）一致后再并入。
- **代码组**：本质是「干活组的扩展池」——准入与干活组相同（能修改本地文件），遵循同样铁律（只收实现/修复任务包、直接落盘、自测+交付说明、组长实测验收）。当任务特征与成员强项匹配时由用户点名指派（如 CLI 级编码任务 → Kimi Code/OpenCode；IDE 内改既有工程 → Cursor/TRAE）；未被点名时默认走干活组现役成员。与干活组共享「一客端一角色」原则。

**ChatParty 顾问双组（2026-09-15 用户定，充分利用 ChatParty 11 站）**：
- **搜索组（无限额，敞开用）**：元宝 · 豆包网页 · 文心一言 · 纳米 · 秘塔。承担资料搜集、大范围初筛、时效核查、背景补充——搜到的资料打包交方案组，不干核心方案活。
- **方案组（有限额，只干核心）**：GLM（智谱清言）· MiMo · Kimi · 千问。只承接方案主笔、互审、定稿、修复方案讨论等最核心环节；资料整理/搜集一律不派，省额度。
- **组内机制**：ChatParty 自带「多模型讨论」（同题群发多站收集观点）与「AI 回答总结/对比」功能——方案组讨论与互审优先用这两个原生机制，组长只做汇总裁决。
- **通道降级**：首选 ChatParty（CDP 9222，看门狗保活）；**用户不愿安装 ChatParty、或其运行影响前台工作时 → 改走 OpenCLI 网页路线**逐站替代，规则与顾问组网页通道一致。

**主流程（单项目闭环）**：

```
组长派任务 → 顾问组写方案 → 顾问组内部多轮互审完善 → 最详细版定稿
→ 干活组制作 → 组长验收 ─┬→ 通过 → 交用户最终验收 → 结案
                         └→ 有 bug → 问题清单反馈顾问组
                            → 顾问组讨论出修复方案 → 干活组修复
                            → 组长复验 →（不过关则重复本循环）
```

**纯文字快车道（2026-09-15 用户定，出品是纯文字时跳过干活组）**：

```
组长派任务 → ChatParty 搜索组集资料 → 方案组讨论出方案
→ 指定方案组其中一位成员定稿并交付 → 组长验收 → 用户终验 → 结案
```

**核心原则**：
1. **方案由顾问组承包**：主笔顾问写具体方案（不是提纲，是可直接照做的详细版），免费/廉价额度承担最烧 token 的环节。
2. **顾问组内部多轮互审取代外部意见**：方案在两位顾问之间往返互审，多轮迭代（退出条件=无实质分歧），干活组不参与方案环节——不提意见、不做终审，只照方案做。
3. **干活组只管制作与修复**：接到的永远是「实现任务包」（附定稿方案全文）或「修复任务包」（附问题清单+修复方案），直接落盘交付。
4. **验收权归组长**：干活组产物由组长实测验收（不轻信交付说明，亲自读文件/跑脚本/实测功能）；组长通过后才交用户最终验收。**用户是最终验收人**——用户不通过，等同 bug，回到修复循环。
5. **修复循环有界**：每轮修复必须由顾问组先出修复方案、组长裁决后才派干活组；同一问题修 3 轮仍不过，组长停下来向用户汇报并给替代建议（如组长代修），不许无限循环烧额度。
6. **一客端一角色**：同一个客户端在同一工作组里只担任一个角色（如 Codex 当组长就不同时当干活）；代码组全员虽具组长资格，但同一项目内当组长就不再兼任代码组/干活组岗位；角色冲突由用户裁决。
7. **组长按任务裁量组队**：调用哪些组由组长根据用户任务的特征决定——可以只调一组，也可以多组组合（如「创意组出稿 → 顾问组并稿 → 代码组实现」三组接力，或「创意组生图方向 + 设计方向」并行出素材）。
8. **组内多成员并行协调 + 直连权限（2026-09-15 放开）**：同一组内可同时调用多名成员协作——互审（顾问组双人往返 / ChatParty 方案组多模型讨论）、出稿对比（创意组多人各出一版择优）、分模块并行开发（代码组按无依赖模块分工）。并行规则：子任务无依赖才并行，有依赖必须串行；并行产物由组长统一收口、合并验收。**直连权限**：顾问组可直接对接顾问组（互审/讨论）、干活组（方案→实现直接交接）、组长、特殊工作组；组长可直连干活组、特殊工作组、顾问组。直连只省中转环节，**验收权不变**——一切产物终验仍归组长实测 → 用户终验。
9. 轻量问答、查询类任务可跳过全流程，组长自答或单点派发。

## 成员管理（增删与准入）

### 角色准入条件
| 角色 | 硬性能力要求 | 备注 |
|---|---|---|
| 组长 | ① 能读写本地文件 ② 能派发任务包并回收产物 ③ 能实测验收（跑脚本/读产物） | 同一时间只有一个组长 |
| 顾问 | ① 能对话产出长方案/长文 ② 不需要对本地文件的写权限 | GUI 网页 AI、CLI 均可 |
| 干活 | ① 能修改本地文件（agent 有文件写权限/工具） ② 能按任务包自测 | CLI agent 或桌面 agent |

### 添加新成员
1. 用户点名某客户端入组 → 组长确认其满足对应角色准入条件
2. 在 `references/capabilities.example.md`（复制为 `capabilities.md` 后使用）建档（能力、额度、调用方式、状态=待实测）
3. 首次派任务用小任务试跑，实测通过后状态改「现役」
4. 调用时一律走「角色」而不是「名字」：说「派干活组」而不是「派 DSH」——这样换成员不用改流程

### 移除成员
1. 用户点名移除（或产物连续验收不通过由组长建议、用户确认）
2. capabilities.md 档案标注「已移出 + 原因 + 教训」，留档备查，不再派任务

### 更换组长
1. **触发**：用户点名（「让 Codex 当组长」）或当前组长不可用（会话结束/额度耗尽）
2. **交接清单**（旧组长输出，新组长接管）：
   - 任务清单与各任务状态（编号/负责人/当前阶段）
   - 已定稿方案与产物所在路径
   - 未闭环的问题（bug 清单、待验收项）
   - 用户验收口径与历史意见
3. 新组长确认清单后接管；交接材料存工作组目录（如 `工作组/交接-<日期>.md`）

## 调度流程（七步）

### 1. 接单与分类
接到任务后先判定类型，查 `references/capabilities.example.md` 的分工决策表：
- **轻量问答/查询** → 组长自答或单点派给一个 AI，不走全流程
- **动手级任务**（编码/制作/成稿/批量处理）→ 走完整七步
- **涉本机文件/系统操作** → 启用 Marvis（特殊工作组·本机组）
- **涉知识库沉淀/公众号素材/长报告播客** → 启用 ima（特殊工作组·本机组）
- **涉图像/视频产出** → 先组长内置生图生视频；质量/风格/工作流有更高要求 → 启用创意组生图生视频方向（即梦/可灵/ComfyUI/VibePaper/Updream；短剧漫剧批量工作流首选 VibePaper）
- **涉 UI 设计稿/原型/视觉规范** → 启用创意组设计方向（Miora/Wegic/Pixso/Figma），产出并入方案定稿
- **编码任务且用户点名代码组成员** → 派代码组（Kimi Code/MiMo Code/TRAE/Cursor/ZCode/OpenCode，规则同干活组）；未点名默认走干活组
- **需资料搜集/时效核查/背景补充** → 派 ChatParty 搜索组（元宝/豆包网页/文心/纳米/秘塔，无限额敞开用），产出资料包交方案组或顾问组
- **出品是纯文字**（文章/文案/报告）→ 走纯文字快车道：搜索组集资料 → 方案组讨论 → 指定一位方案组成员定稿交付（跳过干活组）
- **需大规模并行检索/批量采集（几十上百个子任务）** → 组长代跑

### 2. 拆解与出任务包
组长把任务拆成子任务，每个子任务生成一份「任务包」。

**A. 干活组任务包（结构化模板，机器对机器，必须完整）**——接收方是无平台检测的 CLI agent（DSH 等），结构化反而利于执行：

```
【任务包】
- 任务编号：T01
- 目标：一句话说清要产出什么
- 背景：项目/业务背景 2-5 句
- 输入材料：文件路径 / 粘贴文本 / 上一环产物
- 输出要求：格式、字数、结构、质量标准（越具体越好）
- 边界：不要做什么；禁止编造数据；引用必须标注来源
- 交付形式：纯文本 / 文件 / 代码块 / 直接落盘
```

**B. 顾问组任务包（人话化改写，防平台检测）**——豆包/千问的网页版和客户端都有 AI 内容检测，派发文本必须是「一个人给另一个人下委托」的口吻。组长内部仍按上面模板核对信息完整性，但**出口文本必须重写**：

1. **禁词清单**（出现即破功）：AI、人工智能、模型、大模型、助手、智能体、agent、prompt、指令、任务包、顾问、工作组、组长、任务编号（T01 等）、调度、派发、其他 AI 产品名（豆包/千问/DeepSeek/Kimi/GPT/Claude…）、DSH、WorkBuddy、自动化、系统会。
2. **人称**：自称「我」，称对方「你」。像微信上麻烦同事，不像系统下单。
3. **结构**：先一两句人话背景（为什么找你）→ 要做的事 → 具体要求（可分条，但条目要短、口语）→ 交付方式（「弄好直接发我就行」/「存成 md 发我」）。
4. **去 AI 腔**：不用「首先/其次/综上所述/作为一个…/请确保/值得注意的是」；少排比；标点别过度工整；不加 emoji；不写「如果有疑问请随时告诉我」这类客服句（有疑问他自然会问）。
5. **信息不减**：字数、格式、禁止编造、引用标注等硬约束必须自然融进正文，只是换说法（如「别编资料，拿不准的直接说拿不准」「三千字上下」）。
6. **簿记信息只留组内**：任务编号、依赖关系、验收清单记在组长侧记录里，不进对话文本。
7. **多轮跟进**：同一会话继续用自然短句（「上一版方向对，就改一处：……」），不要重发结构块。
8. **不要解释**：不要求对方「扮演人类」，也不提为什么这样写——自然地以人身份说话即可。

改写示例（同一任务的出口文本）：

```
❌ 机器味：
【任务包】目标：为《书愤》撰写讲解文章…输出要求：2600-3000字…边界：禁止编造…

✅ 人话：
手头在做《书愤》的讲课材料，想请你帮忙写篇讲解文章。学生是高三的，
文章要按上课讲的思路走：从「早岁那知世事艰」一句句带下来，中间把
檀道济那个典故讲透，结尾落到《出师表》上。三千字上下，原诗句一个
字都别动。拿不准的史料别硬写，直接标注出来就行。写完发我，我看看
再跟你对细节。
```

### 3. 派发
派发方式按接收方类型与现场条件选：

**路由决策表（按优先级降级，先查 `references/routes.yaml`，模板见 `routes.example.yaml`）**：

| 优先级 | 通道 | 判据 | 实测案例 |
|---|---|---|---|
| 1 | **官方 CLI / 无头模式** | 客户端自带 CLI（如 `dsh --profile headless`） | DSH 一次性任务全流程 ✅ |
| 2 | **CDP 客户端直连** | Chromium 系客户端接受 `--remote-debugging-port`（bat 重启带开关，登录态保留） | 豆包/千问客户端 CLI 包装实测 ✅，均含 ensure 自愈 + 幂等 bat + 开机自启 VBS。**差异点**：豆包 ProseMirror 认 JS 合成 Enter；千问编辑器必须走 CDP 原生 Input 管线（dispatchKeyEvent） |
| 3 | **OpenCLI 网页** | 网页版 + Bridge 扩展已连（`opencli doctor` 验证） | 千问/豆包网页版 ✅（注意：豆包网页 ProseMirror fill 后 Enter 不认账，需粘贴法） |
| 4 | **剪贴板接力** | 以上均不可用 | GUI 客户端兜底 |
| 5 | **用户中转** | 最终兜底 | 组长输出任务包文本块 |

- **路由不是装了客户端就走客户端**：判据是「通道能否可靠完成任务」。客户端存在但无官方 CLI、无 CDP、逆向成本高 → 优先网页版。CLI-Anything 逆向是最后手段，不是首选。
- **alive=false ≠ 真不可用（2026-09-15 用户定）**：routes.yaml 里 `cdp_alive/proxy_alive: false` 只代表扫描那一刻没在线；若同条目 `ensure_available: true`（有幂等 bat / ensure 自愈 / 启动器），属于「待拉起」——派发时先跑 ensure/bat/launcher 拉起再判，**不要直接降级**。真正不可用 = `installed: false` 或 `ensure_available` 缺失/为 false。运行时通道失败按优先级降级，但降级前必须先试一次拉起。
- **未知工具路由规则（2026-09-15 用户定）**：用户指定 skill 里没有的工具时，**先判断它是网页还是程序**——
  - **网页**（如即梦AI、可灵AI、VibePaper、Updream、各网页版 AI）→ **opencli browser 控制**（Bridge 扩展驱动浏览器；产物网页下载后回传本地路径）。
  - **程序**（本地 exe / 桌面客户端，如 Marvis、Stable Diffusion WebUI、ComfyUI、豆包/千问客户端）→ **CLI-Anything 控制**（按程序暴露的接口选：自带 CLI/API > 启动参数挂 CDP > 命令行收产物）。
  - 判断依据：安装目录/官网形态/是否要开浏览器访问。拿不准就先探测（找 exe 还是找 URL），再按上面二分选通道；探通后把该工具补录进 capabilities.md 对应组。
- **外网成员（web-proxy 通道，双轨规则）**：本地代理 + 独立浏览器 profile（CDP 端口在 routes.yaml 配置）。**ChatGPT**（匿名可对话，回复全链路通）与 **Grok**（投递通，回复需登录）都是国外模型，**双轨规则并行，按任务内容选轨**：①常规顾问轨=任务匹配强项时正常参与主笔/互审（GPT=推理/技术/长文；Grok=语感/选题/时效查证）；②补全轨=国内 AI 缺口时补位（海外信息/知识盲区/合法选题被拒答缩水），强制「先国内尝试 → 缺口清单 → 外网成员补全 → 国内复审」流程。额度保护两轨通用：合并提问、复用会话、调用记任务日志。**硬红线**：两条轨都仅限合法合规内容，不得借外网成员生成违法违规内容，不合规请求直接拒派。
- **顾问组 vs 干活组的差异**：顾问任务（出方案/互审）是纯文本进出，网页通道完全胜任；干活任务需要文件读写/命令执行，必须走优先级 1/2。
- **扫描时机**：安装 skill 后跑 `references/scan_routes.py` 生成 routes.yaml；成员安装/卸载/通道变化后重跑。运行时路线失败按优先级自动降级，不中断任务。

1. **CLI agent 直调**（最可靠）：组长用 shell 执行干活组 CLI 并回收产物（DSH、Codex、Claude Code 等均支持）
2. **CDP 客户端直连**（Chromium 系客户端）：幂等启动 bat 模式——关闭客户端 → 带 `--remote-debugging-port` 重启（默认 profile 保登录）→ WebSocket 驱动渲染页（按各客户端写 CLI 包装：status/new/send/extract/**ensure** 五件套，路径与端口在 `references/routes.yaml` 配置）。**派发前零人工**：直接 `send` 即可（CLI 内部自带 ensure 自愈 + 幂等 bat + 启动文件夹开机自启 VBS）；若 ensure 后仍断（沙箱拉起可能被回收），提示用户双击一次 bat。**编辑器不认 JS 合成 Enter 的客户端必须走 CDP 原生 Input 管线**（dispatchKeyEvent，如千问）；豆包合成/原生皆可。新客户端接入时复制同一模式：idempotent bat + 启动文件夹 VBS + CLI ensure
3. **OpenCLI 网页操控**：`browser <会话> tab new <url>` → `find --role textbox` → `fill` → `keys Enter`；fill 返回 `verified:false` 时（ProseMirror）改剪贴板粘贴
4. **剪贴板接力**（GUI 客户端）：组长把任务包写入剪贴板 + 拉起对应客户端（`start` 进程），用户粘贴发送；产物由用户粘回或给文件路径
5. **用户中转**（兜底）：组长输出任务包文本块，用户手动复制到对应 AI，再把回答粘回来

注意：沙箱拉起的 GUI 进程约 1 分钟被回收（本机实测），**GUI 客户端不要后台挂起等它跑完**——拉起后立即说明「请粘贴任务包」，流程控制权交回用户。规避手段：计划任务/WMI/Cua 代启动（沙箱外进程树），但 Cua 租约在本环境不稳定。

**并行派发规则**：拆解时标记子任务依赖关系——无依赖的子任务可同时派给同组多名成员（每个成员领独立编号的任务包，避免两人做同一件事）；有依赖的必须串行。并行进行中的任务由组长逐个跟收，全部到齐后统一合并验收，再进下一环节。

### 4. 顾问组写方案 + 内部互审（动手级任务必经）

> **多审原则（用户定稿）**：审核轮次投入是全流程性价比最高的环节——方案阶段每多审一轮，干活组返工就少一轮。互审不设固定两轮上限，以「双方无实质分歧」为退出条件（常规 3 轮起步，大型任务 4~5 轮）；组长只在轮次明显空转（连续两轮无新增发现）时叫停裁决。

**第 1 轮——初稿**：组长派「出方案任务包」给主笔顾问（按任务方向选：技术/数据/文档向 → 千问/GPT/Claude 类；语感/选题/产品向 → 豆包/Grok 类），要求输出**完整可执行的具体方案**（技术选型、模块划分、数据结构、文件清单、步骤、接口、边界与风险点），但不写实现代码。

> **文案提醒**：派给顾问的所有文本（初稿委托、互审意见转发、催稿、致谢）一律按上文 B 规则人话化——转发互审意见时也要重写成转述口吻（「另一个朋友看了，提了几个问题：…」），禁止把结构化清单原样粘过去，更禁止出现成员/产品名。

**第 2 轮起——互审循环**：初稿在顾问之间往返互审，每轮一位当审方：
- 审方输出「互审意见清单」：可行性问题、遗漏点、歧义点、可直接补充的细化内容；禁止客套话、禁止只说"没问题"
- 主笔按意见完善，产出下一版（每版带修改说明）
- 退出条件：审方确认无实质分歧（只剩建议值级别的微调可由组长直接拍板并入）

**定稿轮——终稿自检**：定稿前主笔做一遍「实现者视角自检」：假设自己是干活组，逐节问「这一节能直接照做吗？有没有需要我自己做设计决策的地方？」，把答案补进方案。

**定稿标准**：干活组拿到方案后不需要再做任何设计决策——技术栈、目录结构、数据结构、文件清单、验收标准全部齐备。

### 5. 干活组制作
组长把定稿方案全文打包成「实现任务包」派干活组（模板见 `references/playbooks.md`）：
- 交付方式：直接落盘到指定目录（如 `D:\<项目名>\`），不逐文件贴出
- 硬要求：写完自测（文件齐全非空、无语法错误）、交 `交付说明.md`（完成度对照表 + 问题记录 + 遗留点，禁止隐瞒）
- 严格按方案实现不加戏；发现方案有问题记入交付说明，不擅自改

### 6. 组长验收（实测）
- **文件层**：亲自 Read/列目录，核对文件结构与方案第 X 节一致，不轻信交付说明
- **静态层**：JS/Python 语法检查、无 fetch/ES Module/CDN 等方案禁项、数据规模与结构、id/键名对齐
- **行为层**：能跑就跑（无头浏览器冒烟、命令行执行、关键计算用脚本复算）
- **对照层**：逐条过方案里的验收标准，形成「验收报告：通过项 + 问题清单」
- 全部通过 → 交用户最终验收；有问题 → 进入修复循环

### 7. 修复循环（用户或组长验收不通过时）
```
组长出问题清单（编号+复现方式+期望 vs 实际）
→ 顾问组讨论出修复方案（哪改、怎么改、改哪里，逐条对应问题）
→ 组长裁决后派干活组「修复任务包」（问题清单+修复方案全文）
→ 干活组修复落盘 → 组长复验（只验改动项+回归抽查）
→ 不过关重复本循环（同一问题 3 轮上限，超限汇报用户）
→ 通过 → 最终产物交用户验收 → 结案
```

### 监控与收线（贯穿全程）
- 每个子任务有编号和状态：待派发 / 已派发 / 评审中 / 修订中 / 制作中 / 验收中 / 修复中 / 已完成
- 用 TaskCreate/TaskList 跟踪（一个子任务一个 task）
- 交付物按用户习惯存本地指定目录（文件名含时间+项目概述）

## 工作流沉淀与复用（跑通一次 = 固化为资产）

**沉淀时机**：一个项目通过用户最终验收（或关键路径首次跑通），组长立即把本次工作流提炼成模板——不沉淀就白跑。

**模板结构**（存技能目录 `workflows/`，文件名 `YYYYMMDD-<slug>.md`，骨架见 `workflows/TEMPLATE.md`）：
- 任务特征与触发条件（什么任务该套这个模板）
- 调用链（哪些组、哪些成员、什么顺序、哪里并行）
- 各环任务包要点（直接复用 playbooks 模板并注明本项目特化改动）
- 验收要点与坑记录（本次踩过的坑，防止下个项目重踩）
- **可替换变量清单**（项目名/产出路径/内容变量——复用时只改这些）

**复用规则**：
1. 接到新任务先查 `workflows/INDEX.md` 匹配任务特征——命中则**直接调模板**，替换变量即派发，跳过流程设计环节
2. 模板只是起点：本次有新坑/更优路径，跑完回写模板（追加「实跑记录」行），模板越用越准
3. 模板不匹配、又不够格新立模板的，走正常七步流程
4. INDEX.md 是总索引：一行一模板（名称/任务特征/调用链摘要/最后实跑日期），组长接单先扫它

## 分工速查（角色制：名字会换，角色流程不变）

| 任务特征 | 首选 | 理由 |
|---|---|---|
| 方案起草（技术/文档/数据向） | 千问/GPT/Claude 类主笔 + 其他顾问互审（或 ChatParty 方案组多模型讨论） | 免费/廉价额度承担，多轮互审到最详细版 |
| 方案起草（语感/选题/产品向） | 豆包/Grok 类主笔 + 其他顾问互审 | 同上，方向互换 |
| 制作实现（编码/建站/成稿） | 干活组本地直跑（现役 DSH，可换 Codex/Claude Code 等） | 直接落盘 + 组长实测验收闭环 |
| bug 修复 | 顾问组出修复方案 → 干活组执行 | 方案与实现分离，修复有据可依 |
| 产物验收 | 组长实测 → 用户终验 | 组长不轻信交付说明，亲自验证 |
| 长文档精读/合同研报 | 千问/GPT 类顾问或 ima | 长文本 + 知识库溯源 |
| 顾问补位/冗余入口（Kimi 长文本、秘塔联网核查、元宝微信生态等） | ChatParty 网页站聚合（CDP 9222） | 11 站已登录看门狗保活；Kimi/秘塔已实测；派发前查 capabilities.md 站点分工 |
| 大规模并行检索/批量采集 | 组长代跑 | 外部 agent 集群通道已停用 |
| 本机文件整理/系统操作/跨端 | Marvis | 特殊工作组，涉风险操作须用户确认 |
| 知识入库/公众号素材/报告播客 | ima | 特殊工作组，微信生态一键入库 |
| 生图/生视频（轻量） | 组长内置生图生视频能力 | 快、免费、即取即用 |
| 生图/生视频（高质量/图生视频/本地工作流） | 即梦AI / 可灵AI / ComfyUI / Stable Diffusion | 创意组；网页（即梦/可灵）走 opencli browser，程序（ComfyUI/SD）走 CLI-Anything |
| 短剧/漫剧批量生产、分镜到成片全流程 | VibePaper | 创意组；节点式画布改镜头不重渲整条片 |
| B站向视频创作 | Updream | 创意组；B站出品，长记忆+技能库 |
| UI 设计稿/原型/视觉规范 | Miora / Wegic / Pixso / Figma | 创意组；产出作为方案「设计附录」并入定稿 |
| 编码（用户点名代码组） | Kimi Code / MiMo Code / TRAE / Cursor / ZCode / OpenCode | 代码组，规则同干活组：落盘+自测+交付说明+组长实测 |
| 资料搜集/时效核查/背景补充 | ChatParty 搜索组（元宝/豆包网页/文心/纳米/秘塔） | 无限额敞开用；产出资料包交方案组；ChatParty 不可用时降级 OpenCLI 网页路线 |
| 纯文字出品（文章/文案/报告） | 纯文字快车道：搜索组集资料 → 方案组讨论 → 指定一位定稿交付 | 跳过干活组，省流程省额度 |
| 隐私敏感批处理 | 本地干活组（DSH/本地 CLI agent） | 私有底座，数据不出本机 |
| GUI 客户端自动化（拉起/粘贴/收产物） | Cua Driver（MCP，若已接入） | 后台操作键鼠，不抢焦点 |

## Resources

- `references/capabilities.md` — 成员档案（现役/候补/已移出，含额度、入口、调用注意事项）。派发前查这份，不要凭印象分配；新增成员先来这里建档。
- `references/playbooks.md` — 典型工作流剧本（编码工程 / 自媒体内容 / 知识调研 / 合同审阅 / 建站交付），按剧本套任务包模板。
- `workflows/INDEX.md` — 已跑通的工作流模板总索引。接单先查这里，命中直接复用；新模板建成后登记。
- `workflows/TEMPLATE.md` — 工作流模板骨架，沉淀新模板从这里复制。
